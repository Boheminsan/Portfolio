@model SliderImageSingleViewModel
<div class="container mt-4 pt-4">
    <div class="row d-flex justify-content-center">
        <div class="col-md-6">
            <form asp-controller="Slider" asp-action="Create" method="post">
                <div class="mb-3">
                    <label class="form-label" asp-for="Img.ImageName"></label>
                    <input asp-for="Img.ImageName" class="form-control">
                    <span asp-validation-for="Img.ImageName"></span>
                    @* upload *@
                </div>
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" asp-for="Slider.isHome">
                    <label class="form-check-label" asp-for="Slider.isHome">
                        Anasayfada mı? </label>
                </div>
                <button type="submit" class="btn btn-success">Kaydet</button>
            </form>
        </div>
    </div>
</div>